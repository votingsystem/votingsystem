--- preinstalled-issued-certs.pem ---
1) Timestamp server cert.
We need to provide the OCSP server with this certificates to avoid OCSP validation errors with the signatures of the 'TimeStamp Server' and the 'Voting Service Server'. 
